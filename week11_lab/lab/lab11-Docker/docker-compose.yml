version: '3'
services:
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"
  web:
    build: .
    container_name: flask-app
    ports:
      - "4000:80"
    depends_on:
      - redis
